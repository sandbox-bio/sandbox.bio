{
	"name": "sandbox.bio-cloudflare-worker",
	"main": "index.js",
	"scripts": {
		"build": "cd app && npm run build",
		"deploy": "npm run build && wrangler publish --env=stg",
		"deploy-prd": "echo TODO"
	},
	"dependencies": {
		"@cloudflare/kv-asset-handler": "^0.0.9",
		"@supabase/supabase-js": "^1.22.0",
		"itty-router": "^2.4.2"
	}
}
