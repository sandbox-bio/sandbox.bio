name: Sandbox.bio Tests

on:
  push:
    branches: [ dev ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Run tests
    steps:
      - name: Checkout repo
        uses: actions/checkout@v2

      - name: Build
        run: npm install && npm run build

      - name: Run tests
        run: npm run test
